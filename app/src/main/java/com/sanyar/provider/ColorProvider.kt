package com.sanyar.provider

import android.graphics.Color

object ColorProvider {
    val opacityDark: Int = Color.argb(50, 0, 0, 0)
    val lineColor: Int = Color.GRAY
    val accentColor: Int = Color.rgb(44, 68, 109)
    val gray: Int = Color.GRAY
    val textColor: Int = Color.BLACK
    val alphaWhiteColor40 = Color.argb(102, 255, 255, 255)
}